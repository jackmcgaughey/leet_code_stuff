class Solution:
    def checkIfPangram(self, sentence: str) -> bool:
        my_dict = {}
        for x in list(sentence):
            my_dict[x] = 1
        if len(my_dict) < 26:
            return False
        else:
            return True
